# 测试环境配置
spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: 
    
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    
logging:
  level:
    '[com.insurance.claims]': INFO
    '[org.drools]': WARN
    '[org.kie]': WARN
